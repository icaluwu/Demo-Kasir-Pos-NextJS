services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: pos
      POSTGRES_PASSWORD: pos
      POSTGRES_DB: demo_pos
    volumes:
      - demo_pos_pgdata:/var/lib/postgresql/data

volumes:
  demo_pos_pgdata:
